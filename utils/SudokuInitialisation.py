from utils.Sudoku import Sudoku

#besoin d'importer des sudoku premier pouravoir le bon format de sudoku pour convertir
#le csv en sudoku
def init_sudoku(csv):
    sudoku = Sudoku()
    #problem = convert csv to 2d array
    #for i in range(9):
        #for j in range(9):
            #if (problem[i][j] != None)
                #square = sudoku.get_square(i, j)
                #square.set_clue(True)
                #square.set_domain
    return sudoku
